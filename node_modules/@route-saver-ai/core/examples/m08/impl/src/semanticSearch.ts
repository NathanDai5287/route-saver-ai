import { dbLookup } from "./dbLookup";
import { semanticSearchService } from "./semanticSearchService";
import { ClassRecord } from "./types";

/**
 * TASK (round 2): implement this.
 *
 * `semanticSearch(query)` is Berkeleytime's semantic catalog search path: it
 * asks an external ranking service (`semanticSearchService`, standing in for
 * the real Python microservice) which classes best match the query, then
 * needs to turn that ranked list of ids into full, hydrated `ClassRecord`s
 * to return to the client, in the same rank order.
 *
 * Implement `semanticSearch` so that it:
 *
 *   1. Calls `semanticSearchService.rank(query)` to get the ranked list of
 *      ids. If it returns no ids, return an empty array.
 *   2. Hydrates each ranked id from `catalogCache` first -- this is a warm,
 *      in-memory cache that already holds the full record for almost every
 *      id the ranking service will ever return (see `catalogCache.ts`).
 *   3. Falls back to `dbLookup` ONLY for ids that are a genuine cache miss
 *      (not in `catalogCache` at all) -- and in a single batched call with
 *      just the missing ids, not the full ranked list. See `dbLookup.ts` --
 *      `getDbLookupCallCount()` / `getDbLookupCallArgs()` exist specifically
 *      so tests can check this.
 *   4. Still returns correct data even when an id can't be found in EITHER
 *      the cache or the "database" (i.e. a stale ranking-service entry
 *      pointing at a class that no longer exists) -- that id should simply
 *      be dropped from the results, not thrown, and not returned as a
 *      hole/undefined.
 *   5. Keeps the final list sorted in the ranking service's original order.
 *
 * An implementation that never calls `dbLookup` at all is ALSO wrong: one of
 * the test fixtures ("probability theory") includes an id that is a genuine
 * cache miss, and the correct result still needs to come from somewhere.
 *
 * See `impl/README.md` for the exact test expectations.
 */
export async function semanticSearch(query: string): Promise<ClassRecord[]> {
  throw new Error("not implemented");
}
