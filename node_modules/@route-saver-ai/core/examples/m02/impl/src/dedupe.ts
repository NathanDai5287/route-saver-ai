import { RawCourseVersion } from "./types";

/**
 * TASK (round 2): implement this.
 *
 * A single SIS pull can contain multiple "versions" of the same course (see
 * the comment block in fixtures.ts for the exact cases). This periodic sync
 * job upserts one row per course into Berkeleytime's database, so it MUST
 * pick exactly one canonical version per course before writing.
 *
 * Requirements for a correct implementation (see README.md for the full
 * writeup and test/dedupe.test.ts for the executable spec):
 *
 *   1. Two records describe the "same course" only if they refer to the same
 *      distinct catalog entry -- figure out from the fixtures and tests what
 *      that means in practice, and what it deliberately does NOT mean. A
 *      courseId can appear on two records that describe two meaningfully
 *      different catalog entries; those are not a collision and both must
 *      survive as separate output rows.
 *   2. When two records DO describe the same catalog entry, pick exactly
 *      one canonical record between them, based on a consistent policy you
 *      define -- don't just keep whichever happens to come first (or last)
 *      in the input array.
 *   3. The result must be deterministic and idempotent: the same input, in
 *      ANY order, must produce the same output every time (the job reruns on
 *      a schedule -- it can't flip-flop based on incidental array/pagination
 *      order), and running the function again on its own output must be a
 *      no-op.
 *
 * Base your implementation on your own Round 1 design (or a revised version
 * of it, now that you can see the concrete types and fixtures).
 */
export const dedupeCourses = (
  _courses: RawCourseVersion[]
): RawCourseVersion[] => {
  throw new Error("not implemented");
};
